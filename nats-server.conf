# Basic server config
port: 4222
http_port: 8222

# Disable clustering to stop authentication errors
cluster {
  port: 0
}

# Allow all connections (for development)
authorization {
  timeout: 1
}

# Logging
log_time: true
debug: false
trace: false
